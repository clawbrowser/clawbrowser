{
  "name": "clawbrowser",
  "version": "1.0.0",
  "description": "Agent-only browser runtime with browser-managed config.json reuse, CDP sessions, and rotation.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "clawbrowser": {
      "command": "${extensionPath}${/}clawbrowser-mcp",
      "cwd": "${extensionPath}"
    }
  }
}
