# Pinned against Chromium revision 28a7a6c409e03c701d3474ef9e3b1f0be6249039 (151.0.7922.109)
diff --git a/third_party/blink/renderer/modules/peerconnection/peer_connection_dependency_factory.cc b/third_party/blink/renderer/modules/peerconnection/peer_connection_dependency_factory.cc
--- a/third_party/blink/renderer/modules/peerconnection/peer_connection_dependency_factory.cc
+++ b/third_party/blink/renderer/modules/peerconnection/peer_connection_dependency_factory.cc
@@ -28,6 +28,7 @@
 #include "base/trace_event/trace_event.h"
 #include "base/unguessable_token.h"
 #include "build/build_config.h"
+#include "clawbrowser/fingerprint_accessor.h"
 #include "components/webrtc/thread_wrapper.h"
 #include "crypto/openssl_util.h"
 #include "media/base/media_permission.h"
@@ -1109,6 +1110,12 @@ PeerConnectionDependencyFactory::CreatePortAllocator(
     }
   }
 
+  if (clawbrowser::FingerprintAccessor::GetProxy()) {
+    port_config.enable_multiple_routes = false;
+    port_config.enable_nonproxied_udp = false;
+    port_config.enable_default_local_candidate = false;
+  }
+
   std::unique_ptr<webrtc::NetworkManager> network_manager;
   if (port_config.enable_multiple_routes) {
     network_manager = std::make_unique<FilteringNetworkManager>(
diff --git a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
--- a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
+++ b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
@@ -49,6 +49,7 @@
 #include "base/task/thread_pool.h"
 #include "build/build_config.h"
 #include "build/buildflag.h"
+#include "clawbrowser/fingerprint_accessor.h"
 #include "services/metrics/public/cpp/ukm_builders.h"
 #include "services/network/public/cpp/connection_allowlist.h"
 #include "third_party/blink/public/common/features.h"
@@ -301,6 +302,10 @@ webrtc::PeerConnectionInterface::RTCConfiguration ParseConfiguration(
         IceTransportPolicyFromEnum(configuration->iceTransports().AsEnum());
   }
 
+  if (clawbrowser::FingerprintAccessor::GetProxy()) {
+    web_configuration.type = webrtc::PeerConnectionInterface::kRelay;
+  }
+
   switch (configuration->bundlePolicy().AsEnum()) {
     case V8RTCBundlePolicy::Enum::kMaxCompat:
       web_configuration.bundle_policy =
diff --git a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
--- a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
+++ b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
@@ -27,6 +27,7 @@
 #include "base/threading/thread_checker.h"
 #include "base/trace_event/trace_event.h"
 #include "build/chromecast_buildflags.h"
+#include "clawbrowser/fingerprint_accessor.h"
 #include "media/base/media_switches.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/public/platform/platform.h"
@@ -598,6 +599,10 @@ class RTCPeerConnectionHandler::Observer
     DCHECK(native_peer_connection_);
     std::string sdp = candidate->ToString();
     DCHECK(!sdp.empty());
+    if (clawbrowser::FingerprintAccessor::GetProxy() &&
+        sdp.find(" typ relay") == std::string::npos) {
+      return;
+    }
     // The generated candidate may have been added to the pending or current
     // local description, take a snapshot and surface them to the main thread.
     // Remote descriptions are also surfaced because
@@ -634,15 +639,21 @@ class RTCPeerConnectionHandler::Observer
   void OnIceCandidateError(const std::string& address,
                            int port,
                            const std::string& url,
                            int error_code,
                            const std::string& error_text) override {
+    const bool redact_local_address =
+        clawbrowser::FingerprintAccessor::GetProxy() != nullptr;
     PostCrossThreadTask(
         *main_thread_, FROM_HERE,
         CrossThreadBindOnce(
             &RTCPeerConnectionHandler::Observer::OnIceCandidateErrorImpl,
             WrapCrossThreadPersistent(this),
-            port ? String::FromUtf8(address) : String(),
-            static_cast<uint16_t>(port),
-            String::Format("%s:%d", address.c_str(), port),
+            redact_local_address
+                ? String()
+                : (port ? String::FromUtf8(address) : String()),
+            static_cast<uint16_t>(redact_local_address ? 0 : port),
+            redact_local_address
+                ? String()
+                : String::Format("%s:%d", address.c_str(), port),
             String::FromUtf8(url), error_code, String::FromUtf8(error_text)));
   }
