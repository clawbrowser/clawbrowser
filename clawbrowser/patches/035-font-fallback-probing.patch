# Pinned against Chromium revision 28a7a6c409e03c701d3474ef9e3b1f0be6249039 (151.0.7922.109)
diff --git a/third_party/blink/renderer/platform/fonts/font_cache.cc b/third_party/blink/renderer/platform/fonts/font_cache.cc
--- a/third_party/blink/renderer/platform/fonts/font_cache.cc
+++ b/third_party/blink/renderer/platform/fonts/font_cache.cc
@@ -47,2 +47,3 @@
 #include "build/build_config.h"
+#include "clawbrowser/fingerprint_coherence.h"
 #include "skia/ext/font_utils.h"
@@ -158,2 +159,10 @@ const FontPlatformData* FontCache::GetFontPlatformData(
     AlternateFontName alternate_font_name) {
+  // Keep the lowest-level unique-name path fail-closed as well. Today its
+  // web-facing callers are filtered before reaching FontCache, but guarding
+  // here prevents a new caller (or a retained FontMetadata object) from
+  // turning the cache into a local-font oracle.
+  if (alternate_font_name == AlternateFontName::kLocalUniqueFace &&
+      creation_params.CreationType() == kCreateFontByFamily &&
+      clawbrowser::IsLocalFontBlocked(creation_params.Family().Utf8()))
+    return nullptr;
   TRACE_EVENT0("fonts", "FontCache::GetFontPlatformData");
@@ -179,2 +188,6 @@ const SimpleFontData* FontCache::GetFontData(
     AlternateFontName altername_font_name) {
+  if (altername_font_name == AlternateFontName::kLocalUniqueFace &&
+      clawbrowser::IsLocalFontBlocked(family.Utf8()))
+    return nullptr;
+
   if (const FontPlatformData* platform_data = GetFontPlatformData(
diff --git a/third_party/blink/renderer/platform/fonts/font_fallback_list.cc b/third_party/blink/renderer/platform/fonts/font_fallback_list.cc
--- a/third_party/blink/renderer/platform/fonts/font_fallback_list.cc
+++ b/third_party/blink/renderer/platform/fonts/font_fallback_list.cc
@@ -28,6 +28,8 @@
 
 #include "third_party/blink/renderer/platform/fonts/font_fallback_list.h"
 
+#include "clawbrowser/fingerprint_coherence.h"
+
 #include "base/timer/elapsed_timer.h"
 #include "third_party/blink/renderer/platform/font_family_names.h"
 #include "third_party/blink/renderer/platform/fonts/alternate_font_family.h"
@@ -154,9 +156,17 @@ const FontData* FontFallbackList::GetFontData(
 
   for (; curr_family; curr_family = curr_family->Next()) {
     family_index_++;
+    // Local font probing defense: a family the fingerprint does not claim must
+    // not resolve here either. CSSFontSelector already refuses it, but the
+    // FontCache retries below would silently undo that refusal and hand back
+    // the real installed face, leaving the probe wide open.
+    const bool clawbrowser_blocked =
+        !curr_family->FamilyIsGeneric() &&
+        clawbrowser::IsLocalFontBlocked(curr_family->FamilyName().Utf8());
+
     if (!font_selector_) {
       // Don't query system fonts for empty font family name.
-      if (!curr_family->FamilyName().empty()) {
+      if (!curr_family->FamilyName().empty() && !clawbrowser_blocked) {
         if (auto* result = FontCache::Get().GetFontData(
                 font_description, curr_family->FamilyName())) {
           return result;
@@ -168,7 +178,7 @@ const FontData* FontFallbackList::GetFontData(
     const FontData* result =
         font_selector_->GetFontData(font_description, *curr_family);
     // Don't query system fonts for empty font family name.
-    if (!result && !curr_family->FamilyName().empty()) {
+    if (!result && !curr_family->FamilyName().empty() && !clawbrowser_blocked) {
       result = FontCache::Get().GetFontData(font_description,
                                             curr_family->FamilyName());
     }
