{
  "mcpServers": {
    "clawbrowser": {
      "command": "./bin/clawctl",
      "args": ["mcp"],
      "cwd": ".",
      "description": "Managed Clawbrowser lifecycle and identity; CDP endpoints are temporary runtime handles, and clawbrowser://verify/ is the proof source for fingerprint/proxy checks."
    }
  }
}
